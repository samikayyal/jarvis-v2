# 01 — Establish the signed-message control-plane tracer bullet

**What to build:** A valid signed direct-text event from the configured operator crosses the real receiver, durable state, deterministic broker, controlled orchestration adapter, controlled outbound connector, and audit boundary to produce one correlated reply. Establish the Python/`uv` project, deterministic IDs and clocks, typed envelopes, primary automated seam, and a green baseline without connecting live services.

**Blocked by:** None — can start immediately.

**Status:** complete

- [ ] A valid signed operator event produces exactly one correlated reply through the highest external control-plane testing seam.
- [ ] The path persists its request state and required redacted audit evidence using controlled adapters only.
- [ ] The Python project installs, runs, and tests through `uv`, and the full repository remains green.
