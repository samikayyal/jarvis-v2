# Jarvis Calendar — later-version backlog

Google Calendar is intentionally outside Jarvis v1. A later Jarvis version may reintroduce Calendar only through a new scoped design, fresh threat review, explicit OAuth scopes, and independent real-system acceptance. No v1 runtime, protocol, orchestration, configuration, or acceptance contract may depend on this backlog.
